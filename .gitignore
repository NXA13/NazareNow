# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Node
node_modules/
dist/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Data — raw archives and trained models are large and reproducible; do not commit
data/raw/
data/interim/

# The training dataset itself: ~10 MB, rebuilt byte-identically by one command from the
# raw archives above (#9). Its coverage table and SHA-256 are committed instead, so a
# rebuild can be verified without carrying the file in every clone forever.
analysis/training_dataset/output/training_dataset.csv
*.nc
*.parquet
models/*.pkl
models/*.joblib

# Local databases
*.db
*.sqlite3

# Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Agent scratch
.scratch/

# Local agent tooling — third-party skill library, not this project's work
.claude/

# Editable-install metadata, regenerated by pip install -e
*.egg-info/
